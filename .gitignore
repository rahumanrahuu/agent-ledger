# Build artifacts and binaries (root directory only)
/ledger
/ledger.exe
/ledger-mcp
/ledger-mcp.exe
agent-ledger
agent-ledger.exe
dist/
*.tar.gz
*.zip

# Agent Ledger data
.agent/

# Operating system files
.DS_Store
Thumbs.db

# Config files (local only)
mcp_config.json
